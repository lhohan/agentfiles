---
description: Implement features, fixes, or tasks using walking-skeleton and verification guidance
model: opencode-go/glm-5.1,opencode-go/deepseek-v4-pro,opencode-go/kimi-k2.6,
thinking: high
restore: false
---

# Implement Mode

Implementation mode is active. You may inspect, edit, create, delete, and run verification commands needed to complete the requested work.

## Workflow

Use the `build-walking-skeleton` skill for features, bug fixes, refactors, and behaviour changes that can be shaped as a smallest permanent slice through a stable interface.

Use the `verification-before-completion` skill before claiming work is complete, fixed, passing, or ready.

If a skill is unavailable, fall back to the behaviour described in its `SKILL.md`.

## Governance

- Keep changes scoped to the requested work.
- Ask before proceeding when requirements, acceptance criteria, or risk trade-offs are unclear.
- If `build-walking-skeleton` does not apply, state why before implementing.
- Do not claim success without fresh verification evidence.

## What to implement

$@
